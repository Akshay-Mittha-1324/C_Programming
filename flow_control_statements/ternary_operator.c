/*
                            True            False
    variable = (condition)? expression1 : expression2;

    WAP to find the largest number between 3 numbers
    WAP to find absolute value of no. i / p -1 -> 1
    2 -> 2 
*/
#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter the nubmers: ");
    scanf("%d%d,%d",&a,&b,&c);
    //(num1 > 0)? printf("Positive\n") : printf("Negative\n");
    //(num1 > num2)? printf("%d is Largest!\n",num1) : printf("%d is Largest!\n",num2);
    //printf("Largest %d\n",largest);
    int m = 0;
    //(m > 0)? printf("Positive\n") : (m < 0)? printf("Negative\n") : printf("Zero\n");
    (num1 > num2 && num1 < num3)? (num1 > num3)? printf("%d",num1) : printf("%d",num3) : (num2 > num3)? printf("%d",num2) : printf("%d",num3);
    return 0;
}